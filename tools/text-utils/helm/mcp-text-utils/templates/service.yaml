apiVersion: v1
kind: Service
metadata:
  name: {{ include "mcp-text-utils.fullname" . }}
  labels:
    {{- include "mcp-text-utils.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: mcp
      protocol: TCP
      name: mcp
  selector:
    {{- include "mcp-text-utils.selectorLabels" . | nindent 4 }}
