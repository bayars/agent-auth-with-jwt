apiVersion: v1
kind: Service
metadata:
  name: {{ include "mcp-http-caller.fullname" . }}
  labels:
    {{- include "mcp-http-caller.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: mcp
      protocol: TCP
      name: mcp
  selector:
    {{- include "mcp-http-caller.selectorLabels" . | nindent 4 }}
