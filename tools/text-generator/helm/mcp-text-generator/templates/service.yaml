apiVersion: v1
kind: Service
metadata:
  name: {{ include "mcp-text-generator.fullname" . }}
  labels:
    {{- include "mcp-text-generator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: mcp
      protocol: TCP
      name: mcp
  selector:
    {{- include "mcp-text-generator.selectorLabels" . | nindent 4 }}
