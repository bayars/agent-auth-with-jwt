apiVersion: v1
kind: Service
metadata:
  name: {{ include "mcp-fastmcp.fullname" . }}
  labels:
    {{- include "mcp-fastmcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: mcp
      name: mcp
  selector:
    {{- include "mcp-fastmcp.selectorLabels" . | nindent 4 }}
